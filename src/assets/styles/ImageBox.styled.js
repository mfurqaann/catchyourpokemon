import styled from '@emotion/styled'

export const ImageBox = styled.div`
   position: relative;
   text-align: center;
   cursor: pointer;
   height: 100%;
`

export const Image = styled.img`
   width: 120px;
   height: 120px;
`
